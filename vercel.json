{
    "$schema": "https://openapi.vercel.sh/vercel.json",
    "installCommand": "npm install --prefix vertex --include=dev",
    "buildCommand": "npm run prisma:generate --prefix vertex && npm run build --prefix vertex",
    "outputDirectory": "vertex/dist",
    "framework": "vite",
    "functions": {
        "api/trpc/**/*.ts": {
            "includeFiles": "vertex/node_modules/.prisma/**"
        }
    },
    "rewrites": [
        { "source": "/trpc", "destination": "/api/trpc" },
        { "source": "/trpc/:path*", "destination": "/api/trpc/:path*" },
        { "source": "/(.*)", "destination": "/index.html" }
    ],
    "headers": [
        {
            "source": "/chili-wasm/(.*)",
            "headers": [{ "key": "Cache-Control", "value": "public, max-age=31536000, immutable" }]
        },
        {
            "source": "/assets/(.*)",
            "headers": [{ "key": "Cache-Control", "value": "public, max-age=31536000, immutable" }]
        }
    ]
}

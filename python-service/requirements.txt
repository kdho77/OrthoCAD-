# Core dependencies for Phase 1 hybrid manufacturing geometry modules
numpy>=1.24
pydantic>=2.0
trimesh>=4.0
scipy>=1.11   # Required by trimesh for graph/connected_components used in fix_normals, repair, etc.

# For the manufacturing service API (Phase 1+)
fastapi>=0.110
uvicorn[standard]>=0.29
httpx>=0.27

# Optional but recommended for robust GLB/GLTF named mesh handling in some environments
# pygltflib